Underline tabs (Details/Team pattern). Active = green underline + green text.

```jsx
<Tabs value={tab} onChange={setTab} tabs={[
  {value:'details',label:'Details'},
  {value:'team',label:'Team',count:5},
]} />
```
