Segmented view switch (Table/Cards, Week/Month). Active segment = white chip on grey track.

```jsx
<SegmentedControl value={view} onChange={setView}
  options={[{value:'table',label:'Table'},{value:'cards',label:'Cards'}]} />
```
