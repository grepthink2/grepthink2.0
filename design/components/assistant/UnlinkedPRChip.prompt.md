Pill for a pull request with no task on the board, with an "Add task" action.

```jsx
<UnlinkedPRChip pr="PR #44" onAdd={createTask} />
```

The PR number uses the merge purple shared with `PRLinkChip`.
