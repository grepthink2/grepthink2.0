Status/role pill (soft bg + colored text). Use `tone` for statuses, `role` for project roles; gold tone is reserved for achievements/"team set".

```jsx
<Badge tone="success">Submitted</Badge>
<Badge tone="warning">Due soon</Badge>
<Badge role="scrum_master">Scrum Master</Badge>
```
