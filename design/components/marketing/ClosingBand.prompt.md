Dark green closing band (`#0f2a20` + green radial tint + white dot grid) placed before the footer.

```jsx
<ClosingBand
  text="Create a class and import your roster. Every team gets a scrum board and its own channels from day one."
  ctaHref="/select" secondaryHref="/contact" />
```
