Solutions-section pieces: `FeatureColumn` (44px green icon tile, title, description — three across) and `PreviewWindow` (mac-style browser chrome around the 1512×797 product screenshot, cropped from the top with a soft bottom fade).

```jsx
<FeatureColumn icon={<Users size={20} />} title="Smart team formation">Balance teams by interests and skills…</FeatureColumn>
<PreviewWindow src="assets/landing/landing-preview.webp" />
```
