Work-type tag for scrum tasks — 10 fixed presets (`TASK_TAGS`), each with a token-derived soft color.

```jsx
<TagBadge tag="backend" />
<TagBadge tag="bug" onRemove={() => drop('bug')} />
```

Presets: backend · frontend · ui/ux · infra · design · research · bug · chore · optimization · docs.
