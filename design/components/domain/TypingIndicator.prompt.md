Three pulsing dots in a received-message bubble, shown in a thread while a teammate types.

```jsx
<TypingIndicator label="Jordan is typing" />
<TypingIndicator tone="soft" />   // landing-stage grey bubble
```

Dots blink 1.2s with a 0.2s stagger; static under `prefers-reduced-motion`.
