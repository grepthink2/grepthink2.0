Interest-form ranked preference slot (rank chip, project, ↑↓✕ controls; dashed when empty).

```jsx
<RankedProjectSlot rank={1} project="Grepthink 2.0" team="Team 1" onMoveDown={down} onRemove={rm} />
<RankedProjectSlot rank={2} />  {/* empty slot */}
```
