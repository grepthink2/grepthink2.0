Team Status Report form: per-teammate % contribution + positive/constructive feedback, optional scrum-master notes, live 100% total check.

```jsx
<TSRForm entries={entries} onChange={setEntries} showScrumNotes
  scrumNotes={notes} onScrumNotesChange={setNotes} />
```

Wrap in `<Card title="TSR 3" subtitle="Due Feb 01">` and submit via card footer.
