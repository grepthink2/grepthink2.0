# grepthink2.0

Welcome to Grepthink 2.0

## Backend server setup:

1) Create a venv and install dependencies in ```backend/requirements.txt```
2) Run ```python -m uvicorn server:app --reload``` to start the server while in the backend folder